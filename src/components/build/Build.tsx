import Header from "./Header";

const Build = () => {
  return (
    <div
      style={{
        padding: "1rem",
        minHeight: "100vh",
        display: "flex",
        flexDirection: "column",
      }}
    >
      <Header />
    </div>
  );
};

export default Build;
